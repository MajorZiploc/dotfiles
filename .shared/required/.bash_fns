num_of_dirs() {
  ls -Al | egrep "^d" | wc -l
}

num_of_files() {
  ls -Al | egrep "^-" | wc -l
}

num_of_items() {
  ls -A | wc -l
}

parse_git_branch() {
     git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/'
}

